import { ElementRef, EventEmitter, OnInit } from '@angular/core';
export declare class NgXClickOutside implements OnInit {
    private elementRef;
    exclude: string;
    clickOutside: EventEmitter<{}>;
    constructor(elementRef: ElementRef);
    nodesExcluded: Array<HTMLElement>;
    ngOnInit(): void;
    private excludeCheck();
    controlClick($event: any): void;
    onClick($event: any): boolean;
    private shouldExclude(target);
}
