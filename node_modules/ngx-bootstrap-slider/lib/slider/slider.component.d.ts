import { OnInit, EventEmitter } from '@angular/core';
export declare class SliderComponent implements OnInit {
    sliderElement: any;
    slider: any;
    initialOptions: any;
    initialStyle: any;
    style: any;
    value: number | any[];
    valueChange: EventEmitter<{}>;
    min: number;
    max: number;
    step: number;
    precision: number;
    orientation: string;
    range: boolean;
    selection: string;
    tooltip: string;
    tooltipSplit: boolean;
    tooltipPosition: boolean;
    handle: string;
    reversed: boolean;
    rtl: boolean;
    enabled: boolean;
    naturalArrowKeys: boolean;
    ticks: any[];
    ticksPositions: number[];
    ticksLabels: string[];
    ticksSnapBounds: number;
    ticksTooltip: boolean;
    scale: string;
    labelledBy: string[];
    rangeHighlights: any[];
    formatter: Function;
    slide: EventEmitter<{}>;
    slideStart: EventEmitter<{}>;
    slideStop: EventEmitter<{}>;
    change: EventEmitter<{}>;
    constructor();
    private addChangeListeners();
    private prepareSlider();
    private changeAttribute(name, value);
    ngOnInit(): void;
}
